# Basic smoke / load configuration for locust.
# Usage:
#   locust -f loadtest/locustfile.py --host http://localhost:8000 \
#     --users 10 --spawn-rate 2 --run-time 2m --headless \
#     --csv loadtest/results

headless = True
users = 10
spawn_rate = 2
run_time = "2m"
