# Optional. When set, /ask answers are generated by Claude (Anthropic).
# When omitted, the app falls back to returning the retrieved context directly
# (extractive fallback) so the whole pipeline still works without any key.
ANTHROPIC_API_KEY=
