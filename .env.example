# ---------------------------------------------------------------------------
# DevOps RAG Assistant configuration
#
# All values are OPTIONAL. The app ships and runs fully keyless: without any
# configuration it answers from retrieved context (extractive mode). Set
# ANTHROPIC_API_KEY only if you want LLM-generated (synthesized) answers.
# ---------------------------------------------------------------------------

# --- Optional: enable LLM-generated answers (leave empty to run extractive) ---
ANTHROPIC_API_KEY=

# --- Security -----------------------------------------------------------------
# Comma-separated list of allowed CORS origins, e.g. "https://app.example.com".
# Default "*" allows all. Restrict this in production.
CORS_ORIGINS=*
# Shared secret required to call POST /ingest and to read /metrics via the
# X-Admin-Key header. Leave empty to disable auth (local dev only).
ADMIN_API_KEY=

# --- Service ------------------------------------------------------------------
ENV=development
LOG_LEVEL=INFO
# Number of uvicorn worker processes (1 is fine for this app; raise under load).
WORKERS=1

# --- OpenTelemetry (optional tracing backend, e.g. collector:4317) ------------
OTLP_ENDPOINT=
