apiVersion: v1
kind: Service
metadata:
  name: devops-rag-assistant
  labels:
    app: devops-rag-assistant
spec:
  type: ClusterIP
  selector:
    app: devops-rag-assistant
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
